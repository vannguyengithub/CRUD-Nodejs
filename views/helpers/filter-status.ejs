<% filterStatusHelper = (statusFilter) => { %>
    <% statusFilter.forEach( (item) => { %>
        <a class="btn m-b-sm btn-<%= item.class %> btn-sm" href="/admin/items/<%= item.value %>">
            <%= item.name %> (<%= item.count %>)
        </a>
    <% }); %>
<% } %>
